# ===========================================
# Vector - Docker Compose
# ===========================================

services:
  # --- Frontend ---
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
      - INTERNAL_API_URL=http://backend:8000/api/v1
      - NEXTAUTH_URL=http://localhost:3000
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-vector-dev-secret-change-in-production}
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
      - ./frontend/package.json:/app/package.json
      - ./frontend/components.json:/app/components.json
      - ./frontend/tailwind.config.ts:/app/tailwind.config.ts
      - ./frontend/postcss.config.js:/app/postcss.config.js
      - ./frontend/tsconfig.json:/app/tsconfig.json
      - ./frontend/next.config.js:/app/next.config.js
      - ./frontend/.eslintrc.json:/app/.eslintrc.json
    depends_on:
      backend:
        condition: service_healthy

  # --- Backend ---
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/app:/app/app
      - ./backend/alembic:/app/alembic
      - ./backend/alembic.ini:/app/alembic.ini
      - ./backend/tests:/app/tests
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # --- Database ---
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "${DB_PORT:-5433}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vector}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vector}
      POSTGRES_DB: ${POSTGRES_DB:-vector}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vector -d vector"]
      interval: 5s
      timeout: 5s
      retries: 5

  # --- Redis (Task Queue Broker) ---
  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - redisdata:/data

  # --- taskiq worker (executes news pipeline tasks) ---
  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # "app.tasks.taskiq_worker:broker" = broker object for taskiq
    # "app.tasks.taskiq_worker" = module for task auto-discovery
    # If tasks split into multiple modules later, replace with "app.tasks" package.
    command: taskiq worker app.tasks.taskiq_worker:broker app.tasks.taskiq_worker
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://vector:vector@db:5432/vector
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ./backend/app:/app/app
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  # --- taskiq scheduler (sends cron-triggered tasks to Redis) ---
  scheduler:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: taskiq scheduler app.tasks.taskiq_worker:scheduler
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://vector:vector@db:5432/vector
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ./backend/app:/app/app
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
