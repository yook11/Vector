node_modules/
.next/
out/
.turbo/
openapi.json
tsconfig.tsbuildinfo

# Vitest coverage output
coverage/

# Playwright (E2E) artifacts
test-results/
playwright-report/
playwright/.cache/
e2e/.auth/

# 二重防御: ルート .gitignore でもカバーしているが、
# frontend/ ディレクトリ単位でも .env* を明示して commit 事故を構造的に阻止する。
.env
.env.local
.env.*.local
