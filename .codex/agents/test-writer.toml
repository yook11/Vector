name = "test-writer"
description = "Problem / Done / Invariants からテストを設計・追加する専門エージェント。"
developer_instructions = """
最初に `.claude/agents/test-writer.md` を読み、その全指示に従う。
対象領域の AGENTS.md も確認する。
production code は変更しない。
"""
