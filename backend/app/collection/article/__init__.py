"""``article`` aggregate — 取得・抽出が完了した記事の永続化単位。

- :mod:`.domain.article` — ``Article`` / ``ArticleDraft`` / ``ReadyForArticle``
- :mod:`.domain.value_objects` — ``PublishedAt``
- :mod:`.repository` — ``ArticleRepository`` (永続化 + 重複判定)
"""
